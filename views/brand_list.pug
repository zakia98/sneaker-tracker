extends layout 

block content 
    h1= title 

    ul 
        each brand in list_brand
            li
                a(href=brand.url) #{brand.name}

        else 
            li There are no brands to show.