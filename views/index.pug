extends layout

block content
  h1= title
  p Welcome to #[em Sneaker Tracker], a very basic Express website to track sneaker collections.

  h1 Dynamic content

  if error
    p Error getting dynamic content.
  else
    h2 In our database we have the following in records:

    ul
      li #[strong Brands: ] !{data.brand_count}
      li #[strong Models: ] !{data.model_count}
      li #[strong In Stock: ] !{data.inventoryitems_count}
